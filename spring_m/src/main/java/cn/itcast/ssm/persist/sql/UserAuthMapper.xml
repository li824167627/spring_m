<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fm.portal.persist.sql.UserAuthMapper">

	<!-- 插入用户认证信息 -->
	<insert id="selectAuthByUid">
		insert into sec_user_auth
		(id,name,gender,userId,idNo,ext,state,
		actionTime,actionId,actionName,reason)
		value (#{id},#{name},#{gender},#{userId},#{idNo},#{ext},
		#{state},now(),#{actionName},#{reason}
		)
	</insert>
	
	<!-- 插入用户认证日志信息 -->
	

	<!-- 根据uid获取用户审核信息 -->
	

	<!-- 更新用户审核信息 -->
	
	

</mapper>